✔ Changes were stashed on main and restored on release/v0.1 using git stash pop
commands used:
1. git stash -u (to store the uncomitted changes into a temporary stash )
2. git checkout release/v0.1 (to switch from main branch to release branch)
3. git stash pop (to pop out the stash changes in the release branch)
4. git add new-feature.js
5. git commit -m "commited the changes"